name = "cinacoin-demo"
compatibility_date = "2024-01-01"

[build]
command = "cd ../.. && pnpm install && pnpm run build --filter=@cinacoin/core-sdk --filter=@cinacoin/react && cd apps/demo && pnpm run build"
output_directory = ".next"

[vars]
NEXT_PUBLIC_PROJECT_ID = "demo-project-id"
