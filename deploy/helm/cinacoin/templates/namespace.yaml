{{- if eq .Release.Namespace "default" -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Release.Namespace | default "cinacoin" }}
  labels:
    name: {{ .Release.Namespace | default "cinacoin" }}
    managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: cinacoin
{{- end }}
