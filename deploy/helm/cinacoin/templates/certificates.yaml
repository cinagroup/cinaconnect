# TLS Certificate Management — internal service-to-service encryption
# Uses cert-manager with a self-signed CA for internal certificates
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cinacoin-ca
  labels:
    {{- include "cinacoin.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cinacoin-ca-cert
  namespace: cert-manager
spec:
  isCA: true
  commonName: cinacoin-ca
  secretName: cinacoin-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: cinacoin-ca
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cinacoin-ca-issuer
spec:
  ca:
    secretName: cinacoin-ca-secret
---
# Internal certificate for service mesh communication
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cinacoin-internal-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: cinacoin-internal-tls
  issuerRef:
    name: cinacoin-ca-issuer
    kind: ClusterIssuer
  commonName: "*.cinacoin.svc.cluster.local"
  dnsNames:
    - "*.cinacoin.svc.cluster.local"
    - "*.cinacoin.svc"
    - "*.cinacoin"
  renewBefore: 720h
  privateKey:
    algorithm: ECDSA
    size: 256
